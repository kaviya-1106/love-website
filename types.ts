
export interface Milestone {
  date: string;
  title: string;
  description: string;
  icon: string;
}

export interface Memory {
  url: string;
  caption: string;
}

export interface Reason {
  title: string;
  text: string;
}
